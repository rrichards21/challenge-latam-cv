name: 'Continuous Delivery'

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Cloud Run
    runs-on: ubuntu-latest
    
    env:
      PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      CR_REGION: ${{ secrets.GCP_CR_REGION }}
      AR_REGION: ${{ secrets.GCP_AR_REGION }}
      SERVICE_NAME: ${{ secrets.GCP_SERVICE_NAME }}
      GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
      REPO_NAME: challenge-latam-cv

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Decrypt and load GPG key
        run: |
          chmod +x decrypt_gpg.sh
          echo "${{ env.GPG_PASSPHRASE }}" | ./decrypt_gpg.sh
          echo "GCP_SA_CREDENTIALS=$(cat $HOME/secrets/keyfile.json)" >> $GITHUB_ENV

      - name: Authenticate with Google Cloud
        uses: google-github-actions/auth@v3
        with:
          project_id: ${{ env.PROJECT_ID }}
          credentials_json: ${{ env.GCP_SA_CREDENTIALS }}
          create_credentials_file: true

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: |
          gcloud auth configure-docker ${{ env.AR_REGION }}-docker.pkg.dev

      - name: Build and Push Docker Image
        run: |
          IMAGE_URI="${{ env.AR_REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO_NAME }}/${{ env.SERVICE_NAME }}:${{ github.sha }}"
          docker build -t $IMAGE_URI .
          docker push $IMAGE_URI
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE_NAME }}
          image: ${{ env.IMAGE_URI }}
          region: ${{ env.CR_REGION }}
          flags: '--allow-unauthenticated'
